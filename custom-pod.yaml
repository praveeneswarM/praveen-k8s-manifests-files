apiVersion: v1
kind: Pod
metadata:
  name: photo-pod
  namespace: praveen-ns
  labels:
    app: photo-app
spec:
  containers:
    - name: Photo-container
      image: praveenx03/photo-studio:v1.1.0
      ports:
        containerPort: 3000